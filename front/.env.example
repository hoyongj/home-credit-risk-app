# Leave unset for local/Docker dev (nginx proxies /api to the backend
# container). In production (Cloudflare Pages), set this to your Render
# backend's URL, e.g. https://your-api.onrender.com
VITE_API_URL=
